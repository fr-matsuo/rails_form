名前:
<%= @account.first_name %>
<%= @account.last_name %>
<br>

性別:
<%= @account.sex %>
<br>

郵便番号:
<%= @account.post_first %>
-
<%= @account.post_last %>
<br>

都道府県:
<%= @prefecture %>
<br>

メールアドレス:
<%= @account.email %>
<br>

趣味：
<%= @account.hobby %>
<br>

その他の趣味:
<%= @account.other_hobby %>
<br>

意見:
<%= @account.opinion %>

<%= form_for @account, :url => {:action => 'finish'} do |f| %>
  <% [:first_name, :last_name, :sex, :post_first, :post_last, :prefecture_id, :email, :hobby, :other_hobby, :opinion].each do |attr| %>
    <%= f.hidden_field attr %>
  <% end %>
  <%= f.submit '送信', :name => 'toFinish' %>
  <%= f.submit '戻る', :name => 'toNew' %>
<% end %>
